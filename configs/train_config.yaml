model:
  type: random_forest
  params:
    n_estimators: 100
    max_depth: 5
    random_state: 42

training:
  test_size: 0.2
  random_state: 42

paths:
  processed_data: data/processed/insurance_processed.csv
  model_output: models/model.pkl
  metrics_output: models/metrics.json
